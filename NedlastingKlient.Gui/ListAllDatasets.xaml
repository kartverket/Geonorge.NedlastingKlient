<Page x:Class="NedlastingKlient.Gui.ListAllDatasets"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NedlastingKlient.Gui"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Geonorge Nedlasting - Alle datasett">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
    
        <StackPanel Grid.Column="0" Grid.Row="0">
            <Label>Alle datasett</Label>
        </StackPanel>
        <DataGrid Grid.Column="0" Grid.Row="1" AutoGenerateColumns="False" RowHeaderWidth="0" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Navn" />
                <DataGridTextColumn Header="Organisasjon"  />
                <DataGridTextColumn Header="Sist oppdatert"  />
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Column="0" Grid.Row="2">
            <Button Width="100">Vis detaljer</Button>
        </StackPanel>
    </Grid>
</Page>
